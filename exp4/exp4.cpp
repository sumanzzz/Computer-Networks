#include <iostream>
#include <vector>
vector<int> Sender(){
	int n;
	std::cout<<"Enter number of bits:"<<std::endl;
	std::cin>>n;
	
	vector<in> data ;
	vector<int>frame;
	vector<int> add = {0,1,1,1,1,1,1,0};
	
	for(int i=0;i<n;i++){
		int bit;
		std::cin>>bit;
		data.push_back(bit);
	}
	
}
int main(){

}
